#include <vector>
#include <unordered_map>

namespace FastWin{

extern int attributeToOrder;
void orderBy(int attributeToSortBy, std::unordered_map<int, std::vector<std::vector<int>>>& combined_dataset);

}
